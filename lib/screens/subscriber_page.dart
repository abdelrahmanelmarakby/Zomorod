import 'package:flutter/material.dart';
import '../widgets/util.dart';

class Subscriber_page extends StatelessWidget {
  const Subscriber_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SubsScreen();
  }
}
